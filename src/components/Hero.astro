---
import siteConfig from "@/data/site-config.json";

const { cta, ctaHref, image, imageFallback, imageAlt } = siteConfig.hero;
---

{/*
 * Split hero: text left, product image right — LMNT-style layout.
 * Purest's light, airy brand feel instead of LMNT's dark aesthetic.
 */}
<section class="w-full bg-stone-50 overflow-hidden">
  <div class="container">
    <div class="grid md:grid-cols-2 min-h-[480px] md:min-h-[560px] items-center gap-8 py-12 md:py-0">

      {/* Left: headline + description + CTA */}
      <div class="space-y-6 py-8 md:py-16">
        <h1 class="font-serif text-5xl leading-tight tracking-tight md:text-6xl lg:text-7xl">
          Real hydration.<br /><em>Finally.</em>
        </h1>
        <p class="font-serif text-lg text-foreground/70 max-w-sm leading-relaxed">
          Real fruit. Real salt. Nothing else. Purest is the electrolyte you can actually feel — because every ingredient is visible in the jar.
        </p>
        <a
          href={ctaHref}
          class="inline-flex h-16 items-center justify-center rounded-full bg-primary px-14 text-lg font-medium text-primary-foreground transition-all duration-150 hover:bg-primary/90 active:scale-[0.98]"
        >
          {cta}
        </a>
      </div>

      {/* Right: product image */}
      <div class="relative h-[360px] md:h-full md:min-h-[560px] -mx-4 md:mx-0 md:-mr-8 lg:-mr-16">
        <picture>
          <source srcset={image} type="image/webp" />
          <img
            src={imageFallback}
            alt={imageAlt}
            class="absolute inset-0 w-full h-full object-cover object-center"
            loading="eager"
            fetchpriority="high"
          />
        </picture>
      </div>

    </div>
  </div>
</section>
